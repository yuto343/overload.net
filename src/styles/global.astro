<style is:global>
  html {
    font-family: var(--font-normal);
    color: var(--color-white);
  }

  body {
    margin: 0;
  }
  h1,
  h2,
  h3 {
    font-weight: normal;
    margin: 0;
  }

  a {
    text-decoration: none;
    color: inherit;
  }
  a:hover {
    text-decoration: underline;
  }

  ul {
    margin: 0;
    padding: 0;
  }

  ul > li {
    list-style: none;
  }

  p {
    margin: 0;
  }

  button {
    padding: 0;
    border: 0;
    background: none;
    font-family: serif;
    cursor: pointer;
  }
  button:disabled {
    cursor: default;
  }

  b {
    font-weight: normal;
  }
</style>
